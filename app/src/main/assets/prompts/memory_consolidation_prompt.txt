Merge duplicate user memories into canonical Italian lines (manual / future use only).

Input: all active user-facing memories. Output: one line per distinct fact.

Format EXACTLY: (CATEGORY) text
Categories: IDENTITY, PREFERENCE, ROUTINE, FACT

Rules:
- Italian only; never invent facts.
- Merge true duplicates or fragments of the same topic; keep unrelated topics on separate lines.
- NEVER merge user IDENTITY (user's own name) with contact names — homonyms stay separate.
- Pinned facts (if listed separately) must not be merged, rewritten, or dropped.
- NEVER drop a distinct fact — when unsure, keep extra lines.
- ROUTINE: different weekdays or ranges stay separate unless one line states the full week schedule.
- On contradictions, keep the more specific / recent line (input dates help).

No numbering, bullets, headers, or commentary.

CURRENT DATE AND TIME: {{CURRENT_DATETIME}}
