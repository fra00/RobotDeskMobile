package com.example.mydeskrobot.reasoning.model

/**
 * How the robot should interact with the user (robot-only, not phone DND).
 */
enum class RobotProfile {
    NORMAL,
    WORK,
    CALL,
    MEETING,
    FOCUS,
}
